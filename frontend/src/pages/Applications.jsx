import { ArrowUpRight } from 'lucide-react';
import PageHeader, { EmptyState } from '../components/PageHeader';
export default function Applications({ history, onSelect }) { return <><PageHeader eyebrow="Portfolio" title="Applications" description="Previous assessments held by this session." />{!history.length ? <EmptyState message="No previous assessments are available yet."/> : <section className="panel table-panel"><table><thead><tr><th>Applicant</th><th>Prediction</th><th>Default risk</th><th>Risk level</th><th>Recommendation</th><th></th></tr></thead><tbody>{history.map(item => <tr key={item.id} onClick={() => onSelect(item)}><td><strong>{item.applicant.name}</strong><small>{new Date(item.created_at).toLocaleDateString()}</small></td><td>{item.prediction.class}</td><td>{item.prediction.default_probability_percent.toFixed(2)}%</td><td><span className={`status-pill ${item.decision.risk_level.toLowerCase()}`}>{item.decision.risk_level}</span></td><td>{item.decision.recommendation}</td><td><ArrowUpRight size={17}/></td></tr>)}</tbody></table></section>}</>; }
