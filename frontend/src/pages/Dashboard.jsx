import { Link } from 'react-router-dom';
import { CheckCircle2, Plus } from 'lucide-react';
import PageHeader, { Metric } from '../components/PageHeader';

export default function Dashboard({ history }) { const defaults = history.filter(item => item.prediction.class === 'Default').length; const high = history.filter(item => item.decision.risk_level === 'HIGH').length; return <><PageHeader title="Good morning, Alex" description="An overview of current loan application activities" action={<Link className="primary-button" to="/new-assessment"><Plus size={17}/> New assessment</Link>} /><div className="metrics-grid four"><Metric label="Total assessments" value={history.length}/><Metric label="Default predictions" value={defaults} tone="risk"/><Metric label="Non-default predictions" value={history.length-defaults} tone="positive"/><Metric label="High risk" value={high} tone="risk"/></div><section className="dashboard-grid"><div className="panel activity-panel" style={{ gridColumn: '1 / -1' }}><div className="panel-heading"><div><p className="eyebrow">Recent activity</p><h2>Latest assessments</h2></div><Link to="/applications" className="text-link">View all</Link></div>{history.slice(0,4).map(item => <div className="activity-row" key={item.id}><div className="person-badge small">{item.applicant.name[0]}</div><div><strong>{item.applicant.name}</strong><span>{item.prediction.class} · {item.decision.risk_level} risk</span></div><b>{item.prediction.default_probability_percent.toFixed(2)}%</b><CheckCircle2 size={16}/></div>)}{!history.length && <p className="muted">No assessments yet.</p>}</div></section></>; }
