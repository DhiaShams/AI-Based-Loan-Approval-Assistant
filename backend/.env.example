# Exact browser origins allowed to call the API, comma-separated.
CORS_ORIGINS=https://YOUR-VERCEL-DOMAIN.vercel.app,https://ai-loan-approval-assistant.in

# Optional. Keep the default for Vercel preview deployments, or set a narrower regex.
CORS_ORIGIN_REGEX=https://.*\\.vercel\\.app
